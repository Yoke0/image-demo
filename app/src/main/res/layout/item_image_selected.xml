<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="100dp"
    android:layout_height="100dp">

    <ImageView
        android:id="@+id/item_image_select_image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <View
        android:id="@+id/item_image_select_cancel_select"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#80FFFFFF"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="@id/item_image_select_image"
        app:layout_constraintEnd_toEndOf="@id/item_image_select_image"
        app:layout_constraintTop_toTopOf="@id/item_image_select_image"
        app:layout_constraintBottom_toBottomOf="@id/item_image_select_image"/>

    <View
        android:id="@+id/item_image_select_select"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/image_border_select"
        app:layout_constraintStart_toStartOf="@id/item_image_select_image"
        app:layout_constraintEnd_toEndOf="@id/item_image_select_image"
        app:layout_constraintTop_toTopOf="@id/item_image_select_image"
        app:layout_constraintBottom_toBottomOf="@id/item_image_select_image"/>

</androidx.constraintlayout.widget.ConstraintLayout>